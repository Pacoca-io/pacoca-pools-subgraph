{
  "name": "earn-pacoca-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ pacoca-io/pacoca-pools",
    "create-local": "graph create --node http://localhost:8020/ Pacoca-io/earn-pacoca-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ Pacoca-io/earn-pacoca-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Pacoca-io/earn-pacoca-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
